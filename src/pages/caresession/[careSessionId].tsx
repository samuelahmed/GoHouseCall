import { NextPage } from "next";

export const getStaticPaths = async () => {
  //get all care sessions to generate static pages
  //map over all the sessions
  //return the paths object with the params object
};

const CareSession: NextPage = () => {
  return <></>;
};

export default CareSession;
